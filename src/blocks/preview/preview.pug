mixin preview(text,)
  .preview-page__preview.parallax
      img(src="../../images/seed/slide-2.jpg", alt="")
      .preview-page__titlebox
        h1.preview-page__title=text
        +breadcrumb(text)