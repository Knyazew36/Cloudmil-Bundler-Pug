mixin swiper-bottom
  section.section-swiper.mt-section.swiper-bottom
    .section-swiper__inner
      .swiper(data-swiper="bottom")
        .swiper-wrapper
          - for(let a = 1;a < 18; a++)
            .swiper-slide 
              +picture(`swb${a}.jpg`)
